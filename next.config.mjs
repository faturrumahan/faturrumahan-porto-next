/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["placehold.co", "i.imgur.com"], // Add 'placehold.co' to the allowed domains
  },
};

export default nextConfig;
